{
	"$schema": "Exceptions file for guards/check-security.ts. Each exception needs reason, reviewer, and expiry (max ~30 days).",
	"_reviewScope": "check-security auditiert resolveAuditRoots() = Parent-Root (falls vorhanden) UND cwd. Lokal ist das der cosmicdriftgamestudio-Workspace PLUS dieses Repo; in CI existiert kein Parent, also nur dieses Repo. Beim Review BEIDE Baeume pruefen und nicht nur die gehoistete Root-Kopie: `grep -oE '\"<pkg>@[0-9][^\"]*\"' bun.lock` je Root, dann gegen `vulnerable_versions` aus `bun audit --json` halten.",
	"exceptions": []
}

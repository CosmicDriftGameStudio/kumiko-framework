// @runtime client
export const ADMIN_SHELL_FEATURE = "admin-shell" as const;

export const DEFAULT_TENANT_WORKSPACE_ID = "tenant-admin" as const;
export const DEFAULT_PLATFORM_WORKSPACE_ID = "platform" as const;

/** Tenant workspace home — attention cards, query-allowlisted. */
export const TENANT_OVERVIEW_SCREEN_ID = "tenant-overview" as const;

/** Platform workspace home — attention cards, query-allowlisted. */
export const PLATFORM_OVERVIEW_SCREEN_ID = "platform-overview" as const;

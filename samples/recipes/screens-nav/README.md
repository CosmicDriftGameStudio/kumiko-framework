# Screens Nav

Register screens and navigation.

## What it shows

- `r.screen` + `r.nav` with cross-feature parents
- How the admin shell discovers the tree

## Source

Feature entry point: `src/feature.ts`.

## Tests

```bash
cd samples/recipes/screens-nav
bun test
```

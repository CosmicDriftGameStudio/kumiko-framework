/** Product UI mock in the hero `.shot-frame` — not the full-page docs shot. */
export const HERO_SCREENSHOT = {
  src: "/screenshots/hero-app.png",
  alt: "Tasklane planning board — click to enlarge",
  width: 720,
  height: 480,
} as const;

{
  "name": "@kumiko/sample-custom-handlers",
  "description": "Beyond CRUD — when your domain refuses to fit a table.",
  "private": true,
  "dependencies": {
    "@kumiko/framework": "workspace:*"
  }
}

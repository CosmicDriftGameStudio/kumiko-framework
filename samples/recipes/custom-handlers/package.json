{
  "name": "@cosmicdrift/kumiko-sample-custom-handlers",
  "description": "Beyond CRUD — when your domain refuses to fit a table.",
  "private": true,
  "dependencies": {
    "@cosmicdrift/kumiko-framework": "workspace:*"
  }
}

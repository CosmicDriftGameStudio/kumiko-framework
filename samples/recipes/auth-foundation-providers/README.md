# Auth Foundation Providers

Pluggable auth providers on top of auth-foundation.

## What it shows

- Provider registration shape for email/password (and peers)
- How foundation wires sessions + JWT issuance

## Source

Feature entry point: `src/feature.ts`.

## Tests

```bash
cd samples/recipes/auth-foundation-providers
bun test
```

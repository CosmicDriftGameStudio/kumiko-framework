{
  "name": "@cosmicdrift/kumiko-sample-writeform-section",
  "description": "A projectionDetail `writeForm` section rendered beside an established entityEdit form on the same page, so the shared form chrome (footer, submit affordance) between the two can be verified side by side.",
  "private": true,
  "type": "module",
  "scripts": {
    "e2e": "bun x playwright test --config=e2e/playwright.config.ts",
    "screenshot": "bun x playwright test --config=e2e/playwright.config.ts"
  },
  "dependencies": {
    "@cosmicdrift/kumiko-framework": "workspace:*",
    "@cosmicdrift/kumiko-renderer": "workspace:*",
    "@cosmicdrift/kumiko-renderer-web": "workspace:*",
    "react": "^19.2.6"
  },
  "devDependencies": {
    "@cosmicdrift/kumiko-server-runtime": "workspace:*",
    "@playwright/test": "^1.62.1",
    "@tailwindcss/cli": "^4.3.0",
    "@types/react": "^19.2.14",
    "tailwindcss": "^4.3.0"
  }
}

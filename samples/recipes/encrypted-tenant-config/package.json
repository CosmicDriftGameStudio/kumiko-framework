{
  "name": "@cosmicdrift/kumiko-sample-encrypted-tenant-config",
  "description": "Per-tenant secrets via config-key encryption — Stripe-API-key pattern.",
  "private": true,
  "dependencies": {
    "@cosmicdrift/kumiko-bundled-features": "workspace:*",
    "@cosmicdrift/kumiko-framework": "workspace:*"
  }
}

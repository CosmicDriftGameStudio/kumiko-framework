import type { ClientFeatureDefinition } from "@cosmicdrift/kumiko-renderer-web";
import { driverTranslations } from "../i18n";

export const driverClient: ClientFeatureDefinition = {
  name: "demo-driver",
  translations: driverTranslations,
};

import type { ClientFeatureDefinition } from "@cosmicdrift/kumiko-renderer-web";
import { configDemoTranslations } from "../i18n";

export const configDemoClient: ClientFeatureDefinition = {
  name: "config-demo",
  translations: configDemoTranslations,
};

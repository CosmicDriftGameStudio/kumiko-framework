---
"@cosmicdrift/kumiko-renderer-web": patch
---

nav-tree: register `upload` in the `NAV_ICONS` registry (lucide-react `Upload`). App authors can now reference `icon: "upload"` in `r.nav` declarations without falling back to the blank dot.

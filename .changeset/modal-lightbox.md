---
"@cosmicdrift/kumiko-renderer": minor
"@cosmicdrift/kumiko-renderer-web": minor
---

renderer: `ModalShell` shared overlay primitive, `LightboxProps` + `DefaultLightbox` for full-size image dialogs, and Apex landing click-to-enlarge via vanilla JS/CSS (no React dependency in static pages).

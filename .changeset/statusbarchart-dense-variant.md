---
"@cosmicdrift/kumiko-renderer-web": minor
---

StatusBarChart gains a `dense` prop: fixed-size flat-fill bars without gradients/tick marks, sized to fit a table cell instead of stretching to `w-full`.

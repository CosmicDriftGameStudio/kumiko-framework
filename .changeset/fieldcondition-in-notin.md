---
"@cosmicdrift/kumiko-types": minor
"@cosmicdrift/kumiko-framework": minor
---

fw#2756: `FieldCondition` gains `{ field, in }` and `{ field, notIn }` set-membership variants alongside the existing `eq`/`ne`, evaluated by `evalFieldCondition`.

---
"@cosmicdrift/kumiko-dev-server": patch
"create-kumiko-app": patch
---

Scaffolded demo `tasks` feature ships i18n keys, client translations, and list edit rowAction so `bun dev` boots on current validators.

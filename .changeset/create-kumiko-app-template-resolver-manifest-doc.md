---
"create-kumiko-app": patch
---

The generated feature manifest's `template-resolver` description now mentions the `markdown` content format, matching the upstream fix in `@cosmicdrift/kumiko-bundled-features`.

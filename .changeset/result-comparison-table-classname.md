---
"@cosmicdrift/kumiko-renderer-web": minor
---

ResultTable + ComparisonTable: optionales `className`-Prop (auf das `<table>` gemergt), damit Consumer table-spezifisches Styling ergänzen können — z.B. bespoke Share-Deck-Layouts (`deck-table`) oder Original-Klassen (`min-w-*`, `[&_th]:text-xs`) beim Umstieg von hand-getailwindeten Tabellen auf das Widget.

---
"@cosmicdrift/kumiko-renderer-web": patch
---

test(primitives): export pure helpers for unit testing — `computeVisiblePages`, `defaultCellRender`, `isComponentRendererRef` (index.tsx) and `parseIso`/`toIso` (date-input). No behaviour change; mirrors money-input which already exports its pure logic.

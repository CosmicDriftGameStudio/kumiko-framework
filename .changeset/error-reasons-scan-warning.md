---
"@cosmicdrift/kumiko-dev-server": patch
---

infra#285/#286 triage: rename `ScanWarning.reason` to `.message` (a `console.warn` display string, not a wire error code) — `guard-error-reasons` now actually scans this package instead of silently skipping it.

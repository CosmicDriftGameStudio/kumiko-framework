---
"@cosmicdrift/kumiko-cli": minor
---

Adds a `@cosmicdrift/kumiko-cli/repo-manifest` subpath exporting `repoManifestSchema`, `loadRepoManifest`, and `RepoManifestError` for reading a repo's `kumiko.json` layout manifest (fw#2856).

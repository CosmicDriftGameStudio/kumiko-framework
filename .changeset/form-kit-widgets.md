---
"@cosmicdrift/kumiko-renderer-web": minor
---

Form-Kit-Widgets: `useDraft`, `NumberField`/`MoneyField`/`PercentField`, `ResultPanel`/`ResultTable` und ein `emphasize`-Flag für `DetailList`. Bausteine für das Rechner-Muster der Consumer-Apps (Zahlenfelder → pure Funktion → Live-Ergebnispanel), das bislang nur als `type:"custom"`-JSX mit dupliziertem Field/Input-Boilerplate und handgebauten `<dl>`/`<table>` existierte. Siehe kumiko-framework#925.

---
"@cosmicdrift/kumiko-framework": minor
---

Anonymous extraRoutes get `write`, running as the session the /api chain resolved for this request (never more than that caller could do via /api/write, request-resolved tenant, /api/ only).

<!-- kumiko-changes
feature: framework
type: improvement
title: Anonymous extraRoutes get `write`, running as the session the /api chain resolved for this request (never more than that caller could do via /api/write, request-resolved tenant, /api/ only).
-->

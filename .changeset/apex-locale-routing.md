---
"@cosmicdrift/kumiko-headless": minor
---

Add `@cosmicdrift/kumiko-headless/locale-routing` with `createLocaleRouter` for configurable apex URL locale routing (`detectLang`, `publicPath`, `altLocalePath`, `sectionAnchor`, legacy `localeHints`).

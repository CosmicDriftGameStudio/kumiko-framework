---
"@cosmicdrift/kumiko-types": minor
"@cosmicdrift/kumiko-renderer-web": minor
---

Add `mic` and `circle-stop` to the `NavIconKey` vocabulary, with matching lucide-react entries in `NAV_ICONS`. Lets voice-recording controls (e.g. the AI agent panel's speech input) render as `icon`/`size="icon"` buttons instead of falling back to text labels that crowd the input row.

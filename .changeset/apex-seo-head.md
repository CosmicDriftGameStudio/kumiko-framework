---
"@cosmicdrift/kumiko-headless": minor
---

Apex: add SEO fields to `<head>` — `robots`, `og:site_name`, `og:locale`, `twitter:card`, `preconnect`, and JSON-LD.

All new fields on `ApexHead` are optional; existing callers need no changes.

---
"@cosmicdrift/kumiko-renderer": minor
"@cosmicdrift/kumiko-renderer-web": minor
---

`RenderEdit` renders wizard-mode screens (`layout.mode: "wizard"`) one section per step, with a progress indicator, Back/Next/Finish buttons, and per-step field validation instead of rendering all sections at once.

---
"@cosmicdrift/kumiko-renderer-web": patch
---

WorkspaceShell accepts `navBadges` (same NavTree slot as DefaultAppShell) so multi-workspace apps can show runtime badges (e.g. unread inbox counts).

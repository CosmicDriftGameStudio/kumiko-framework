---
"@cosmicdrift/kumiko-bundled-features": minor
---

Add `form-draft` bundled feature: save/resume in-progress multi-step form state (`save`/`discard`/`get`), scoped per tenant + user, with a `form-draft-user-data` companion feature for GDPR export/erasure coverage.

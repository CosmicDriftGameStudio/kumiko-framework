---
"@cosmicdrift/kumiko-renderer-web": patch
---

`DefaultForm` centers its content (`max-w-3xl mx-auto`) instead of pinning it to the left. On wide screens the auto-UI form no longer sits in the left half with an empty right side.

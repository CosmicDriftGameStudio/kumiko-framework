// Hero demo — nutzdaten live in scripts/demo-kit/demos/create-app/.
// Hydration produces the same DemoDef record-demo + E2E consume.

import { loadDemo } from "../demo-kit/engine/load-demo";

export const createAppDemo = loadDemo("create-app");

export default createAppDemo;

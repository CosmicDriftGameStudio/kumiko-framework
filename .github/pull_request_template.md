## What & why

## Testing
- [ ] `bun kumiko check` passes locally
- [ ] New/changed behavior has a test (recipe test for framework features)
- [ ] Consumers break without changing their own code (rendered ARIA roles, testids, error shapes, generated types, projection columns)? Then the changeset says so, in its own sentence.

## Linked issue
Closes #
